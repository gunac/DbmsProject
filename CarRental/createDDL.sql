CREATE TABLE CARTYPE (CARTYPECODE VARCHAR(255) NOT NULL, CARTYPENAME VARCHAR(255), SEATINGINFO VARCHAR(255), PRIMARY KEY (CARTYPECODE))
CREATE TABLE RENTAL (RENTID INTEGER NOT NULL, DAILYRATE DOUBLE, DROPOFFDAY DATE, LOCATIONDESCRIPTION VARCHAR(255), PICKUPDAY DATE, RENTALDAYS INTEGER, SUBTOTAL DOUBLE, TAXESANDFEES DOUBLE, TOTALPRICE DOUBLE, CARTYPECODE_CARTYPECODE VARCHAR(255), PRIMARY KEY (RENTID))
CREATE TABLE CARFEATURE (FEATUREID INTEGER NOT NULL, DESCRIPTION VARCHAR(255), PRIMARY KEY (FEATUREID))
CREATE TABLE CARMODEL (MODELID INTEGER NOT NULL, COUNT INTEGER, MODELNAME VARCHAR(255), CARTYPECODE_CARTYPECODE VARCHAR(255), PRIMARY KEY (MODELID))
CREATE TABLE CUSTOMER (CUSTOMERID INTEGER NOT NULL, DOB DATE, EMAIL VARCHAR(255), LICENSENO VARCHAR(255), NAME VARCHAR(255), PASSWORD VARCHAR(255), PRIMARY KEY (CUSTOMERID))
CREATE TABLE ORDERS (ORDERID INTEGER NOT NULL, DAILYRATE DOUBLE, DROPOFFDAY DATE, LOCATIONDESCRIPTION VARCHAR(255), PICKUPDAY DATE, RENTALDAYS INTEGER, SUBTOTAL DOUBLE, TAXESANDFEES DOUBLE, TOTALPRICE DOUBLE, CUSTOMERID_CUSTOMERID INTEGER, MODELID_MODELID INTEGER, PRIMARY KEY (ORDERID))
ALTER TABLE RENTAL ADD CONSTRAINT FK_RENTAL_CARTYPECODE_CARTYPECODE FOREIGN KEY (CARTYPECODE_CARTYPECODE) REFERENCES CARTYPE (CARTYPECODE)
ALTER TABLE CARMODEL ADD CONSTRAINT FK_CARMODEL_CARTYPECODE_CARTYPECODE FOREIGN KEY (CARTYPECODE_CARTYPECODE) REFERENCES CARTYPE (CARTYPECODE)
ALTER TABLE ORDERS ADD CONSTRAINT FK_ORDERS_CUSTOMERID_CUSTOMERID FOREIGN KEY (CUSTOMERID_CUSTOMERID) REFERENCES CUSTOMER (CUSTOMERID)
ALTER TABLE ORDERS ADD CONSTRAINT FK_ORDERS_MODELID_MODELID FOREIGN KEY (MODELID_MODELID) REFERENCES CARMODEL (MODELID)
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT DECIMAL(38), PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 0)
